# required metadata
sonar.projectKey=SonarWebTool
sonar.projectName=SonarWebTool
sonar.projectVersion=1.0
 
# path to source directories (required)
sonar.sources=src/main/java/
 
# path to test source directories (optional)
sonar.tests=src/test/java/ar/com/globallogic/sonarWebTool

 
# path to project binaries (optional), for example directory of Java bytecode
sonar.binaries=target/classes/
 
# optional comma-separated list of paths to libraries. Only path to JAR file and path to directory of classes are supported.
sonar.libraries=
 
# The value of the property must be the key of the language.
sonar.language=java

sonar.jacoco.reportPath=jacoco.exec
sonar.core.codeCoveragePlugin=jacoco
sonar.jacoco.reportPath=tests/jacoco-exec/jacoco.exec
sonar.dynamicAnalysis=reuseReports
sonar.surefire.reportsPath=tests/test-reports

sonar.verbose=true
 
# Additional parameters
